mode: ContinuousDeployment
next-version: 0.1.0
branches:
  master:
    regex: ^master$
    increment: Minor
  features:
    regex: ^features[/-]
    increment: Patch
  pull-request:
    regex: ^(pull|pr)[/-]
    tag: PullRequest
    increment: Inherit
ignore:
  sha: []