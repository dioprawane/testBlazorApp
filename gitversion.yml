mode: ContinuousDeployment
branches:
  master:
    regex: ^master$
    increment: Minor
    prevent-increment-of-merged-branch-version: true
  features:
    regex: ^features[/-]
    increment: Patch
ignore:
  sha: []
